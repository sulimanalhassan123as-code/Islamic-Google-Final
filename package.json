{
  "name": "islamic-google-functions",
  "version": "1.0.0",
  "dependencies": {
    "@google/generative-ai": "^0.1.3"
  }
}```

---

#### **File 5 of 6: `api/search.js` (The Search Backend)**
(IMPORTANT: This file now goes into a new folder named `api`. Use the GitHub trick: type `api/search.js` in the file name box.)

```javascript
// This is the backend function for Google Search
const fetch = require('node-fetch'); // Vercel may require this

module.exports = async (req, res) => {
  const { q: query } = req.query;
  const apiKey = process.env.GOOGLE_API_KEY;
  const searchEngineId = process.env.GOOGLE_SEARCH_ENGINE_ID;
  const apiUrl = `https://www.googleapis.com/customsearch/v1?key=${apiKey}&cx=${searchEngineId}&q=${encodeURIComponent(query || 'islam')}`;

  try {
    const response = await fetch(apiUrl);
    const data = await response.json();
    res.status(200).json(data);
  } catch (error) {
    res.status(500).json({ error: 'Failed to fetch search results.' });
  }
};
